import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Clinical Portal 2.0",
    template: "%s | Clinical Portal 2.0",
  },
  description: "HIPAA & GDPR compliant clinical portal for modern healthcare management. Streamline patient care from registration to discharge.",
  keywords: ["healthcare", "clinical", "portal", "HIPAA", "GDPR", "patient management", "EMR"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
